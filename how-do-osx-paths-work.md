# osx and zsh

starts with `.zshenv`

then this process adds some more:

```
/etc/zprofile -> /usr/libexec/path_helper

which adds:

    /etc/paths
    /etc/paths.d/*
```

then `.zprofile` gets added
